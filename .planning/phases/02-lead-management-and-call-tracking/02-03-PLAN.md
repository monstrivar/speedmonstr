---
phase: 02-lead-management-and-call-tracking
plan: 03
type: execute
wave: 3
depends_on:
  - 02-02
files_modified:
  - app/src/components/ui/sheet.tsx
  - app/src/components/LeadDetailSheet.tsx
  - app/src/components/FollowUpPrompt.tsx
autonomous: true

must_haves:
  truths:
    - "Tapping a lead opens a bottom sheet at 85dvh with full lead details"
    - "Sheet contains a Ring button as an anchor tag wrapping the tel: href"
    - "Sheet contains Follow up and Not relevant action buttons"
    - "After returning from a call, a prompt appears asking the outcome"
    - "Sheet renders correctly on iPhone (no content cut off by home indicator)"
  artifacts:
    - path: "app/src/components/ui/sheet.tsx"
      provides: "shadcn Sheet component"
      contains: "SheetContent"
    - path: "app/src/components/LeadDetailSheet.tsx"
      provides: "Bottom sheet with lead detail, call button, and action buttons"
      exports: ["LeadDetailSheet"]
    - path: "app/src/components/FollowUpPrompt.tsx"
      provides: "Post-call outcome selector"
      exports: ["FollowUpPrompt"]
  key_links:
    - from: "app/src/components/LeadDetailSheet.tsx"
      to: "app/src/hooks/useCallTracking.ts"
      via: "useCallTracking hook"
      pattern: "useCallTracking"
    - from: "app/src/components/LeadDetailSheet.tsx"
      to: "app/src/hooks/useLeadMutations.ts"
      via: "useUpdateLeadStatus hook"
      pattern: "useUpdateLeadStatus"
    - from: "app/src/components/LeadDetailSheet.tsx"
      to: "app/src/components/FollowUpPrompt.tsx"
      via: "showFollowUp prop"
      pattern: "FollowUpPrompt"
---

<objective>
Build the lead detail bottom sheet and the follow-up outcome prompt. These are the two new UI surfaces in Phase 02.

Purpose: Users need a place to see full lead details, initiate a call, and record what happened. Both components are pure UI — they consume the hooks from Plan 02.
Output: LeadDetailSheet and FollowUpPrompt components, plus shadcn Sheet added to the project.
</objective>

<execution_context>
@/Users/ivarknutsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ivarknutsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-lead-management-and-call-tracking/02-RESEARCH.md
@.planning/phases/02-lead-management-and-call-tracking/02-02-SUMMARY.md
@app/src/components/LeadCard.tsx
@app/src/types/index.ts
@app/src/locales/no.json
@app/src/locales/en.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shadcn Sheet component</name>
  <files>app/src/components/ui/sheet.tsx</files>
  <action>
    Check if app/src/components/ui/sheet.tsx already exists. If it does, skip this task.

    If it does not exist, run from the app/ directory:
    ```bash
    cd app && npx shadcn@latest add sheet --yes
    ```

    This generates app/src/components/ui/sheet.tsx with Sheet, SheetContent, SheetHeader, SheetTitle, SheetDescription, SheetFooter, SheetClose sub-components.

    Verify the file was created and exports SheetContent.
  </action>
  <verify>ls app/src/components/ui/sheet.tsx returns the file path without error</verify>
  <done>app/src/components/ui/sheet.tsx exists and exports SheetContent.</done>
</task>

<task type="auto">
  <name>Task 2: LeadDetailSheet + FollowUpPrompt components</name>
  <files>
    app/src/components/LeadDetailSheet.tsx
    app/src/components/FollowUpPrompt.tsx
  </files>
  <action>
    **FollowUpPrompt.tsx:**

    Props: { open: boolean; onOutcome: (outcome: "answered" | "no_answer" | "voicemail" | "cancelled") => void }

    Use shadcn Sheet with side="bottom" (reuses existing component, no AlertDialog needed):
    - SheetContent: className="rounded-t-2xl pb-safe" style={{ paddingBottom: "env(safe-area-inset-bottom)" }}
    - SheetTitle: t("followUp.title") — "Ringte du dem?"
    - Four buttons stacked vertically (full-width, variant outline except "answered" which is default):
      - "Fikk svar" → onOutcome("answered")
      - "Ikke svar" → onOutcome("no_answer")
      - "Telefonsvar" → onOutcome("voicemail")
      - "Avbryt" → onOutcome("cancelled")
    - Use t() for all button labels
    - The sheet is NOT closeable by swipe/backdrop (add onOpenChange that calls onOutcome("cancelled") — ensures state cleanup)

    **LeadDetailSheet.tsx:**

    Props:
    ```typescript
    interface LeadDetailSheetProps {
      lead: Lead | null
      open: boolean
      onOpenChange: (open: boolean) => void
      userId: string
      organizationId: string
    }
    ```

    Internal logic:
    - Call useCallTracking(lead?.id ?? "", userId) — guard lead?.id with empty string fallback
    - Call useUpdateLeadStatus(organizationId) — destructure mutate as updateStatus, isPending

    Layout (SheetContent side="bottom" className="h-[85dvh] rounded-t-2xl overflow-y-auto" style={{ paddingBottom: "env(safe-area-inset-bottom)" }}):

    1. SheetHeader with SheetTitle = full name (first_name + last_name), subtitle = received date formatted with Intl.DateTimeFormat "no"
    2. Lead details section (each item only if value exists):
       - Phone: phone icon + phone number
       - Email: mail icon + email
       - Company: building icon + company
       - Description: text block
       - Source badge: t("lead.source") + lead.source
    3. Call button (Ring button):
       - Render as: <Button asChild size="lg" className="w-full gap-2"><a href={`tel:${lead.phone}`} onClick={handleCallPress}><Phone /> {t("lead.callNow")}</a></Button>
       - Only render if lead.phone exists
       - onClick fires handleCallPress synchronously (fire-and-forget); the anchor's default navigates to the phone dialer
       - Also trigger Capacitor Haptics: import { Haptics, ImpactStyle } from "@capacitor/haptics"; call Haptics.impact({ style: ImpactStyle.Medium }) inside handleCallPress via a wrapper
    4. Divider (Separator component)
    5. Action buttons row:
       - "Fulgt opp" button: onClick calls updateStatus({ leadId: lead.id, status: "fulgt_opp" }), disabled if isPending
       - "Ikke relevant" button: variant="outline" onClick calls updateStatus({ leadId: lead.id, status: "ikke_relevant" }), disabled if isPending
    6. Render FollowUpPrompt: open={showFollowUp} onOutcome={handleFollowUpOutcome}

    Add i18n keys to app/src/locales/no.json and app/src/locales/en.json:

    no.json additions:
    ```json
    "followUp": {
      "title": "Ringte du dem?",
      "answered": "Fikk svar",
      "noAnswer": "Ikke svar",
      "voicemail": "Telefonsvar",
      "cancel": "Avbryt"
    },
    "lead": {
      "receivedLabel": "Mottatt"
    }
    ```

    en.json additions:
    ```json
    "followUp": {
      "title": "Did you reach them?",
      "answered": "Got through",
      "noAnswer": "No answer",
      "voicemail": "Left voicemail",
      "cancel": "Cancel"
    },
    "lead": {
      "receivedLabel": "Received"
    }
    ```

    Note: Merge these into the existing JSON structure — do not replace existing keys.

    Import Separator from @/components/ui/separator (already installed). Import all lucide icons (Phone, Mail, Building2) from lucide-react.
  </action>
  <verify>cd app && npx tsc --noEmit — no errors in LeadDetailSheet.tsx or FollowUpPrompt.tsx</verify>
  <done>Both components compile cleanly. LeadDetailSheet renders full lead info with Ring anchor, two action buttons, and mounts FollowUpPrompt when showFollowUp is true.</done>
</task>

</tasks>

<verification>
- app/src/components/ui/sheet.tsx exists
- app/src/components/LeadDetailSheet.tsx and FollowUpPrompt.tsx compile with zero TypeScript errors
- Ring button uses &lt;a href="tel:..."&gt; inside Button asChild (not window.open or onClick navigation)
- SheetContent uses h-[85dvh] and paddingBottom env(safe-area-inset-bottom) (not h-full or 100vh)
- i18n keys added to both no.json and en.json
- cd app && npx tsc --noEmit passes
</verification>

<success_criteria>
LeadDetailSheet and FollowUpPrompt exist as standalone components that compile cleanly. They are not yet rendered anywhere — wiring happens in Plan 04.
</success_criteria>

<output>
After completion, create .planning/phases/02-lead-management-and-call-tracking/02-03-SUMMARY.md
</output>
