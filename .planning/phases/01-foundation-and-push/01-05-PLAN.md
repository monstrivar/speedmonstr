---
phase: 01-foundation-and-push
plan: 05
type: execute
wave: 4
depends_on: ["01-03", "01-04"]
files_modified:
  - app/src/components/Layout.tsx
  - app/src/pages/DashboardPage.tsx
  - app/.gitignore
autonomous: false

must_haves:
  truths:
    - "Capacitor iOS project exists and builds in Xcode"
    - "Push notification hook is wired into the authenticated app"
    - "App runs in iOS Simulator showing login page"
    - "App builds for physical device with push entitlement"
  artifacts:
    - path: "app/ios/"
      provides: "Capacitor iOS Xcode project"
    - path: "app/.gitignore"
      provides: "Git ignore for iOS build artifacts"
  key_links:
    - from: "app/src/components/Layout.tsx"
      to: "app/src/hooks/usePushNotifications.ts"
      via: "usePushNotifications(user.id) call"
    - from: "app/capacitor.config.ts"
      to: "app/ios/"
      via: "npx cap sync ios"
---

<objective>
Wire push notifications into the app, add Capacitor iOS platform, build and verify in Xcode.

Purpose: Complete the end-to-end flow from app scaffold to running iOS app with push capability.
Output: iOS project that builds in Xcode, push hook connected, ready for device testing.
</objective>

<execution_context>
@/Users/ivarknutsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ivarknutsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-push/01-RESEARCH.md
@.planning/phases/01-foundation-and-push/01-03-SUMMARY.md
@.planning/phases/01-foundation-and-push/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire push hook and build iOS project</name>
  <files>
    app/src/components/Layout.tsx
    app/src/pages/DashboardPage.tsx
    app/.gitignore
    app/ios/
  </files>
  <action>
    1. Update app/src/components/Layout.tsx (or DashboardPage.tsx -- wherever makes sense):
       - Import usePushNotifications from @/hooks/usePushNotifications
       - Call usePushNotifications(user.id) where user comes from useAuth
       - This registers for push on app startup after authentication

    2. Create app/.gitignore:
       ```
       node_modules/
       dist/
       .env
       .env.local
       
       # Capacitor iOS - keep project but ignore build artifacts
       ios/App/Pods/
       ios/App/build/
       ios/App/App.xcworkspace/xcuserdata/
       ios/App/DerivedData/
       ```

    3. Build the web app:
       ```
       cd app && npm run build
       ```

    4. Add iOS platform and sync:
       ```
       cd app && npx cap add ios
       cd app && npx cap sync ios
       ```

    5. Verify iOS project was created:
       ```
       ls app/ios/App/App.xcworkspace
       ```

    6. Open Xcode project to verify it builds (Claude can run this but user needs to check):
       ```
       cd app && npx cap open ios
       ```
       Note: Xcode must be installed. If not available, document that user needs to open manually.

    7. Important: The iOS project needs Push Notifications capability added in Xcode:
       - Target > Signing & Capabilities > + Capability > Push Notifications
       - Target > Signing & Capabilities > + Capability > Background Modes > Remote notifications
       This MUST be done manually in Xcode by the user.
  </action>
  <verify>
    ls app/ios/App/App.xcworkspace -- iOS project exists.
    cd app && npm run build -- web build still works.
    cat app/.gitignore | grep "Pods" -- iOS build artifacts ignored.
  </verify>
  <done>iOS project created, push hook wired into authenticated app, web build synced to iOS project.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 01 app: Vite + React 19 + TypeScript scaffold, Supabase integration (client, auth, types, schema), dashboard with realtime lead feed, push notification pipeline (client hook + Edge Function), and Capacitor iOS project.
  </what-built>
  <how-to-verify>
    1. **Web dev server:** Run `cd app && npm run dev` -- opens on localhost:5174, shows login page
    2. **Build:** Run `cd app && npm run build` -- exits cleanly, no errors
    3. **iOS project:** Open `app/ios/App/App.xcworkspace` in Xcode
       - Select your development team in Signing & Capabilities
       - Add Push Notifications capability (+ Capability > Push Notifications)
       - Add Background Modes capability > check "Remote notifications"
       - Build for Simulator (Cmd+B) -- should succeed
    4. **Schema:** Review `app/supabase/schema.sql` -- ready to run in your Supabase project
    5. **Edge Function:** Review `app/supabase/functions/push-notification/index.ts` -- APNs sender

    **To fully test push notifications, you will need to:**
    - Create a Supabase project and run schema.sql
    - Set VITE_SUPABASE_URL and VITE_SUPABASE_ANON_KEY in app/.env
    - Deploy the Edge Function to Supabase
    - Configure APNs key in Supabase Edge Function secrets (APNS_TEAM_ID, APNS_KEY_ID, APNS_PRIVATE_KEY)
    - Create a database webhook on leads INSERT pointing to the Edge Function
    - Build and run on a physical iOS device (push does not work in Simulator)
  </how-to-verify>
  <resume-signal>Type "approved" if Xcode build succeeds and structure looks good, or describe any issues.</resume-signal>
</task>

</tasks>

<verification>
- `cd app && npm run build` exits 0
- `ls app/ios/App/App.xcworkspace` exists
- usePushNotifications is called in Layout or DashboardPage
- All 5 plans have been executed successfully
- Schema SQL, Edge Function, client hooks, UI pages all present
</verification>

<success_criteria>
- iOS project builds in Xcode (Simulator target)
- Push hook is wired into authenticated flow
- All Phase 01 code is complete and buildable
- User can proceed to Supabase setup and device testing
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-push/01-05-SUMMARY.md`
</output>
