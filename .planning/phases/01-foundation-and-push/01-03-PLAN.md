---
phase: 01-foundation-and-push
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified:
  - app/src/App.tsx
  - app/src/pages/LoginPage.tsx
  - app/src/pages/DashboardPage.tsx
  - app/src/components/Layout.tsx
  - app/src/components/LeadCard.tsx
  - app/src/hooks/useLeads.ts
autonomous: true

must_haves:
  truths:
    - "User sees a login page with email input and magic link button"
    - "After auth, user sees a dashboard with a realtime lead feed"
    - "New leads appear in the feed without page refresh"
    - "Each lead shows name, description, status badge, and a Call button"
    - "All visible text comes from i18n t() function"
    - "Layout is mobile-first (Capacitor target is phone)"
  artifacts:
    - path: "app/src/pages/LoginPage.tsx"
      provides: "Magic link login UI"
      min_lines: 30
    - path: "app/src/pages/DashboardPage.tsx"
      provides: "Lead feed dashboard"
      min_lines: 40
    - path: "app/src/components/LeadCard.tsx"
      provides: "Individual lead display card"
      min_lines: 25
    - path: "app/src/hooks/useLeads.ts"
      provides: "TanStack Query + Supabase Realtime hook for leads"
      exports: ["useLeads"]
  key_links:
    - from: "app/src/hooks/useLeads.ts"
      to: "app/src/lib/supabase.ts"
      via: "supabase.from('leads').select()"
      pattern: "supabase.*from.*leads"
    - from: "app/src/hooks/useLeads.ts"
      to: "app/src/lib/supabase.ts"
      via: "supabase.channel() realtime subscription"
      pattern: "supabase.*channel"
    - from: "app/src/pages/DashboardPage.tsx"
      to: "app/src/hooks/useLeads.ts"
      via: "useLeads() hook call"
    - from: "app/src/App.tsx"
      to: "app/src/hooks/useAuth.ts"
      via: "useAuth() for route protection"
---

<objective>
Build the login page, dashboard with realtime lead feed, and mobile-first layout.

Purpose: Create the core UI that users interact with -- login via magic link, see leads as they arrive in realtime.
Output: Working login flow and dashboard with LeadCard components updating via Supabase Realtime.
</objective>

<execution_context>
@/Users/ivarknutsen/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ivarknutsen/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-push/01-RESEARCH.md
@.planning/phases/01-foundation-and-push/01-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Login page, layout, and routing</name>
  <files>
    app/src/App.tsx
    app/src/pages/LoginPage.tsx
    app/src/components/Layout.tsx
  </files>
  <action>
    1. Create app/src/pages/LoginPage.tsx:
       - Use shadcn/ui Card, Input, Button components
       - Email input field with placeholder from t("auth.email_placeholder")
       - "Send magic link" button using t("auth.send_magic_link")
       - On submit: call signIn(email) from useAuth hook
       - Show success message t("auth.magic_link_sent") after sending
       - Show loading state on button while sending
       - Mobile-first: centered card, max-w-sm, padding for phone screens
       - All text via useTranslation() t() function
       - Named export: export function LoginPage()

    2. Create app/src/components/Layout.tsx:
       - Simple mobile-first layout wrapper
       - Top bar with app name t("app.name") and sign out button
       - Sign out uses signOut from useAuth
       - Main content area with padding
       - Safe area insets for iOS (env(safe-area-inset-top) etc via Tailwind)
       - Named export: export function Layout({ children })

    3. Update app/src/App.tsx:
       - Import useAuth hook
       - If loading: show centered spinner/loading text t("common.loading")
       - If no user: show LoginPage
       - If user: show Layout wrapping DashboardPage
       - Use React Router Routes with:
         - "/" -> DashboardPage (protected)
         - "/login" -> LoginPage
         - Navigate to /login if not authenticated
       - Named export: export function App()
  </action>
  <verify>
    cd app && npm run build -- no TS errors.
    Grep for 'useTranslation' in LoginPage.tsx -- must be present.
    Grep for 'export function' in all new files -- all use named exports.
  </verify>
  <done>Login page renders with email input and magic link button. Layout wraps authenticated content. Router protects dashboard behind auth.</done>
</task>

<task type="auto">
  <name>Task 2: Dashboard page with realtime lead feed</name>
  <files>
    app/src/pages/DashboardPage.tsx
    app/src/components/LeadCard.tsx
    app/src/hooks/useLeads.ts
  </files>
  <action>
    1. Create app/src/hooks/useLeads.ts following the RESEARCH.md pattern exactly:
       - Accept organizationId parameter (string)
       - useQuery with queryKey ["leads", organizationId]
       - queryFn: supabase.from("leads").select("*").eq("organization_id", organizationId).order("created_at", { ascending: false }).limit(50)
       - useEffect: subscribe to supabase.channel("leads-realtime") with postgres_changes INSERT on leads table filtered by organization_id
       - On INSERT event: queryClient.invalidateQueries({ queryKey: ["leads", organizationId] })
       - Cleanup: supabase.removeChannel(channel) on unmount
       - Return the query result
       - Named export: export function useLeads(organizationId: string)

    2. Create app/src/components/LeadCard.tsx:
       - Accept lead prop typed as Lead from @/types
       - Use shadcn/ui Card component
       - Show: lead.first_name + lead.last_name (bold), lead.description (truncated to 2 lines)
       - Badge showing lead.status using t("dashboard.status." + lead.status) for translated status
       - "Ring" (Call) button with Phone icon from lucide-react
       - Call button: window.open(`tel:${lead.phone}`) -- works natively in Capacitor
       - Timestamp: use Intl.DateTimeFormat with "no" locale for created_at
       - Mobile-first: full-width card with proper padding
       - Named export: export function LeadCard({ lead })

    3. Create app/src/pages/DashboardPage.tsx:
       - Use useAuth to get user, then look up organization_id (for now, hardcode a TODO comment: organization_id will come from user profile lookup)
       - For MVP: pass a placeholder organizationId to useLeads (or handle the case where it's not yet available)
       - If loading: show loading state
       - If no leads: show empty state with t("dashboard.no_leads")
       - If leads: map over leads rendering LeadCard for each
       - Title: t("dashboard.title")
       - Use ScrollArea from shadcn/ui for the lead list
       - Named export: export function DashboardPage()
  </action>
  <verify>
    cd app && npm run build -- no TS errors.
    Grep for 'supabase.channel' in useLeads.ts -- realtime subscription present.
    Grep for 'invalidateQueries' in useLeads.ts -- cache invalidation present.
    Grep for 'useTranslation' in DashboardPage.tsx and LeadCard.tsx -- i18n used.
    Grep for 'tel:' in LeadCard.tsx -- call action present.
  </verify>
  <done>Dashboard shows realtime lead feed. LeadCard displays lead info with call button. Realtime subscription invalidates TanStack Query cache on new leads. All text via i18n.</done>
</task>

</tasks>

<verification>
- `cd app && npm run build` exits 0
- All components use named exports
- All user-visible strings use t() from react-i18next
- useLeads subscribes to Supabase Realtime channel
- LeadCard has tel: link for calling
- Login page uses shadcn/ui components
- Layout handles iOS safe area insets
</verification>

<success_criteria>
- Login page: email input + magic link button, success message after send
- Dashboard: scrollable list of LeadCards with name, description, status, call button
- Realtime: new leads appear without refresh via Supabase channel subscription
- i18n: all strings from no.json, translatable
- Mobile-first: designed for phone screen in Capacitor
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-push/01-03-SUMMARY.md`
</output>
